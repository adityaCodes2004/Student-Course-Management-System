## SQL – Student Course Management System

- Created 3 tables: Students, Courses, Enrollments
- Used primary & foreign keys to model relationships
- Inserted sample data and tested with SELECT, JOIN, and GROUP BY queries
- Implemented ANSI SQL so it runs on SQLite / MySQL / PostgreSQL etc.
